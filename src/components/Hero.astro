---
import { Image } from 'astro:assets';
import bckg from '../assets/images/bckg.jpeg';
import avatarMarta from '../assets/images/avatar_Marta.webp';
import avatarBeta from '../assets/images/avatat_beta.webp';

// Add a tiny placeholder version of the background image
const tinyPlaceholder = 'data:image/jpeg;base64,...'; // Base64 encoded tiny version of your background
---

<section class="relative overflow-hidden min-h-[90vh] flex items-center">
  <!-- Background image with enhanced gradient overlay -->
  <div class="absolute inset-0 z-0">
    <!-- Multiple layered gradients for depth and uniformity -->
    <div class="absolute inset-0 bg-gradient-to-br from-primary-900/90 via-primary-800/75 to-black/70 mix-blend-multiply"></div>
    <div class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent"></div>
    
    <!-- Add blur placeholder -->
    <div 
      class="absolute inset-0 bg-cover bg-center blur-xl scale-110 transform"
      style={`background-image: url(${tinyPlaceholder})`}
    ></div>
    
    <Image 
      src={bckg} 
      alt="Background"
      width={1920}
      height={1080}
      quality={80}
      loading="eager"
      priority={true}
      class="w-full h-full object-cover scale-110 animate-subtle-zoom opacity-35"
    />
    
    <!-- Enhanced decorative elements -->
    <div class="absolute inset-0 bg-[radial-gradient(circle_at_top_right,rgba(255,255,255,0.15),transparent_50%)]"></div>
    <div class="absolute inset-0 bg-[radial-gradient(circle_at_bottom_left,rgba(0,0,0,0.4),transparent_70%)]"></div>
    <div class="absolute bottom-0 left-0 right-0 h-32 bg-gradient-to-t from-black/60 to-transparent"></div>
    
    <!-- Subtle pattern overlay for texture -->
    <div class="absolute inset-0 opacity-10 bg-[url('data:image/svg+xml;base64,PHN2ZyB4bWxucz0iaHR0cDovL3d3dy53My5vcmcvMjAwMC9zdmciIHdpZHRoPSI1IiBoZWlnaHQ9IjUiPgo8cmVjdCB3aWR0aD0iNSIgaGVpZ2h0PSI1IiBmaWxsPSIjZmZmIj48L3JlY3Q+CjxyZWN0IHdpZHRoPSIxIiBoZWlnaHQ9IjEiIGZpbGw9IiNjY2MiPjwvcmVjdD4KPC9zdmc+')]"></div>
  </div>
  
  <!-- Content -->
  <div class="container mx-auto px-4 relative z-10 pt-20">
    <div class="grid md:grid-cols-2 gap-12 items-center">
      <!-- Text content with card effect -->
      <div class="backdrop-blur-sm bg-white/10 p-8 md:p-10 rounded-2xl border border-white/20 shadow-2xl transform md:translate-x-12 animate-slide-in">
        <h1 class="text-4xl md:text-5xl font-serif font-semibold text-white mb-6 leading-tight">
          Profesjonalne Wsparcie <span class="text-primary-300">McKenzie Friend</span> dla Polaków w UK
        </h1>
        <p class="text-xl text-gray-100 mb-8 font-light">
          Ekspercka pomoc w sprawach rodzinnych, pomagamy polskojęzycznym rodzinom w poruszaniu się po brytyjskim systemie prawnym.
        </p>
        <div class="flex flex-col sm:flex-row gap-4">
          <a 
            href="/kontakt" 
            class="bg-primary-600 text-white px-8 py-3 rounded-lg hover:bg-primary-500 transition-all duration-300 shadow-lg hover:shadow-primary-500/30 hover:-translate-y-1"
          >
            Darmowa Konsultacja
          </a>
          <a 
            href="/uslugi" 
            class="bg-white/10 backdrop-blur-sm text-white border border-white/30 px-8 py-3 rounded-lg hover:bg-white/20 transition-all duration-300 shadow-lg hover:-translate-y-1"
          >
            Nasze Usługi
          </a>
        </div>
      </div>
      
      <!-- Experts avatars -->
      <div class="relative h-[400px] hidden md:block">
        <!-- First expert -->
        <div class="absolute -top-10 right-12 animate-float-1">
          <div class="relative flex items-center gap-6">
            <!-- Caption on the left -->
            <div class="w-64">
              <div class="bg-black/40 p-4 rounded-xl shadow-lg">
                <p class="text-white font-medium mb-2">Grażyna Modzelewska</p>
                <p class="text-white text-sm leading-snug font-light">
                  "Jestem głosem najmłodszych – ich bezpieczeństwo to kompas, który wyznacza każdą decyzję."
                </p>
              </div>
            </div>
            <!-- Avatar -->
            <div class="relative">
              <div class="absolute -inset-0.5 bg-gradient-to-br from-primary-300 to-primary-600 rounded-full blur opacity-70"></div>
              <img 
                src={avatarMarta.src} 
                alt="Marta - McKenzie Friend Expert" 
                class="relative w-64 h-64 rounded-full border-2 border-white/80 shadow-xl object-cover"
              />
            </div>
          </div>
        </div>

        <!-- Second expert -->
        <div class="absolute top-44 right-32 animate-float-2">
          <div class="relative flex items-center gap-6">
            <!-- Avatar -->
            <div class="relative">
              <div class="absolute -inset-0.5 bg-gradient-to-br from-primary-300 to-primary-600 rounded-full blur opacity-70"></div>
              <img 
                src={avatarBeta.src} 
                alt="Beta - McKenzie Friend Expert" 
                class="relative w-56 h-56 rounded-full border-2 border-white/80 shadow-xl object-cover"
              />
            </div>
            <!-- Caption on the right -->
            <div class="w-64">
              <div class="bg-black/40 p-4 rounded-xl shadow-lg">
                <p class="text-white font-medium mb-2">Beata Modzelewska</p>
                <p class="text-white text-sm leading-snug font-light">
                  "Wiedza to Twój najsilniejszy oręż – bronię Twoich praw, byś mógł iść naprzód z godnością."
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  
  <!-- Scroll indicator -->
  <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 z-10 animate-bounce">
    <a href="#services" class="text-white opacity-80 hover:opacity-100 transition-opacity">
      <svg class="w-8 h-8" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
      </svg>
    </a>
  </div>
</section>

<style>
  @keyframes subtle-zoom {
    0% { transform: scale(1.1); }
    50% { transform: scale(1.15); }
    100% { transform: scale(1.1); }
  }
  
  @keyframes float {
    0% { transform: translateY(-6px); }
    50% { transform: translateY(-12px); }
    100% { transform: translateY(-6px); }
  }
  
  @keyframes slide-in {
    from { opacity: 0; transform: translateX(-20px); }
    to { opacity: 1; transform: translateX(0); }
  }
  
  .animate-subtle-zoom {
    animation: subtle-zoom 20s infinite alternate ease-in-out;
  }
  
  .animate-float {
    animation: float 6s infinite ease-in-out;
  }
  
  .animate-slide-in {
    animation: slide-in 1s ease-out forwards;
  }

  @keyframes float-1 {
    0% { transform: translate(0, 0); }
    50% { transform: translate(-5px, -8px); }
    100% { transform: translate(0, 0); }
  }

  @keyframes float-2 {
    0% { transform: translate(0, 0); }
    50% { transform: translate(5px, -8px); }
    100% { transform: translate(0, 0); }
  }

  .animate-float-1 {
    animation: float-1 8s infinite ease-in-out;
  }

  .animate-float-2 {
    animation: float-2 10s infinite ease-in-out;
  }
</style>